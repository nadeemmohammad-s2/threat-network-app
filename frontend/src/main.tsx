import React from 'react';
import ReactDOM from 'react-dom/client';
import ThreatNetworkApp from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ThreatNetworkApp />
  </React.StrictMode>
);